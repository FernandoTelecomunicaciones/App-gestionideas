<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG BUILDS ONLY: a scriptable command surface for verifying the reminder engine with adb. Never in release. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name="com.fernando.ahora.debug.DebugCommandReceiver"
            android:exported="true" />
    </application>
</manifest>
